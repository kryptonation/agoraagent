[project]
name = "morekick"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DD", email = "dilipd@conceptvines.com" }
]
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.43.34",
    "fastapi>=0.138.0",
    "langchain>=1.3.10",
    "langchain-aws>=1.6.0",
    "langgraph>=1.2.6",
    "pydantic>=2.13.4",
    "sse-starlette>=3.4.5",
    "uvicorn>=0.49.0",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
